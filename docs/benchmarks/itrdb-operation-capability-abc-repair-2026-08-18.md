# ITRDB A/B/C 前沿裁决修复验证（2026-08-18）

## 结论

本轮在不放宽自动应用语义、不增加窗口宽度的前提下，修复了三类下游覆盖问题：

1. 候选锚定的正向终端 lag 阶梯不再因更老侧多一个同向层级而被缺轮仲裁覆盖。
2. `partialMove` 与“按缺轮逐轮复核”明确共享窗口时，最终定位重排会同步两种解释的窗口。
3. 多事件平均窗口不得排除其操作所依赖的稳定终端断点；冲突时仅做保持 13 年宽度的最小平移。

对应提交：

- `a6bc6311` 已修复 C 类伪轮阶梯被缺轮仲裁覆盖
- `afd9cb08` 已统一局部移动与缺轮复核的最终前沿窗口
- `03c1d81b` 已阻止多事件平均窗口排除稳定终端断点

## 冻结留出结果

协议：`itrdb-operation-capability-v4-1000`。使用同一组 24 个未污染文件、每类 974 个冻结案例，按文件聚类 bootstrap 计算单侧 95% 下界。

| 类别 | 正确建议 / 前沿诊断 | 工作流建议准确率 | 单侧 95% 下界 | 串行恢复率 | 响应率 | 条件窗口覆盖率 | 保存重开稳定率 |
|---|---:|---:|---:|---:|---:|---:|---:|
| A | 911 / 974 | 93.53% | 91.01% | 93.53% | 99.38% | 96.61% | 100% |
| B | 2428 / 2645 | 91.80% | 90.34% | 83.04% | 99.28% | 96.31% | 100% |
| C | 2285 / 2518 | 90.75% | 88.38% | 78.17% | 98.93% | 93.23% | 100% |

A、B 的观察值和单侧下界均达到 90%。C 的观察值达到 90%，但文件聚类下界仍未达到 90%，不能表述为 C 已通过统计门槛。

结果目录：

- A/B：`D:\软件测试\itrdb-operation-capability\results\v4-ab-24file-final-abc-repair-20260818`
- C：`D:\软件测试\itrdb-operation-capability\results\v4-c-24file-final-terminal-boundary-guard-20260817`

两次正式运行均为 `errors=0`。冻结 manifest 仍报告未选入本轮的 `az086` 源 SHA 不匹配；本轮 24 文件集合已排除该文件。

## 相对改进

C 类相对本轮开始时的冻结结果：

- 正确建议：`2252 -> 2285`，增加 33 次。
- 工作流建议准确率：`90.19% -> 90.75%`。
- 文件聚类单侧 95% 下界：`87.85% -> 88.38%`。
- 串行恢复率：`77.04% -> 78.17%`。
- 完整恢复案例：`729 -> 741`。
- 保存重开稳定率保持 100%。

A 保持 `911/974`。B 从此前 `2431/2647 = 91.84%` 变为 `2428/2645 = 91.80%`，单侧下界仍通过 90%，变化很小但未隐藏。

## 剩余边界

C 类剩余 233 次失败：

| 原因 | 数量 |
|---|---:|
| 主窗口未覆盖正确前沿 | 92 |
| 提示了较老的后续事件 | 74 |
| 操作或位移错误 | 40 |
| 拒答 | 27 |

主要残余是近距缺轮链被压缩为 aggregate partial 后，定位仍落在较老事件；其次是伪轮窗口偏移。扩大内部虚拟搜索的实验对 10 个代表失败没有改善，已在提交前撤回，未进入生产代码。

## 安全门禁

- 紧凑 A/B/C/D：B `33/37`、C `41/42`、D `30/30`，与修复前一致。
- 缺轮控制：`69/69`。
- 整体移动控制：排除已知 `az086` SHA 错误后 `19/19`。
- co612 首轮正确窗口：`25 >= 22`。
- co612 clean review：`2/56`。
- co612 joint：`45/45` 完全一致。
- 相关 Vitest：437 项通过。
- `npm run build`：通过。
