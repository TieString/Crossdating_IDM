# co612 缺轮前沿与局部移动解释契约（2026-08-12）

## 目的

恢复生产自适应参考路线在 co612 全部自然 0 同时删除场景中的串行复核能力，
同时把相邻或近邻多缺轮与连续缺段的物理不可区分性显式交给样本证据裁决。
自动诊断仍只输出一个主建议；替代解释只在独立复合证据通过门槛时出现。

## 输入与隔离

- 输入：`D:\软件测试\数据\ITRDB\itrdb_download\measurements\northamerica\usa\co612.rwl`
- SHA-256：`b1d4756303eb1c8af9805e5f14a95b7e4e04c6ef18ada0a86aa9dedf715af048`
- 56 条序列，358 个自然 0；全部事件绝对年份可识别。
- 运行前后 SHA-256 相同，源文件未修改。
- 参考模式：`adaptive`，即 pairwise 冷启动后使用 COFECHA-pass/master 自举。
- 结果目录：`D:\软件测试\co612-review-window-results\adaptive-full-zero-frontier-contract-2026-08-12`

## 生产约束

1. 未经阶段验证的较新侧原始 partial 峰不得重定位累计缺轮前沿。
2. 当多参考芯已验证同一区域的 `missing + partial` 复合结构、累计位移一致、
   区域不超过 13 年且不存在独立整体移动基线时，可附加多缺轮解释。
3. 替代解释只复用已定位的唯一前沿窗口，不伪造尚未逐个定位的缺轮年份。
4. 用户依据断裂、腐朽等实物证据在连续缺段与逐轮缺轮之间切换；切换本身不编辑数据。
5. 基准只有在所选主解释或受约束替代解释操作正确且唯一窗口覆盖真年份时，
   才模拟用户确认一个事件并进入下一轮。

## 正式从零结果

| 指标 | 结果 |
| --- | ---: |
| 串行确认恢复 | 351/358（98.04%） |
| 历史冻结结果 | 346/358（96.65%） |
| 提升 | +5 个事件，+1.39 个百分点 |
| 主解释确认 | 345 |
| 受约束替代解释确认 | 6 |
| 确认时 Top1 | 253/351（72.08%） |
| 确认窗口中位宽度 / P90 | 9 / 13 年 |
| clean review | 2/56（3.57%） |
| 初始 / 最终 zero-lag | 24.58% / 98.32% |
| 初始 / 最终绝对 lag P90 | 7 / 0 年 |

`mtr741:1723` 的主解释为 `partialMove -2`，唯一窗口 `1720-1726`、Top1
`1723`；同一窗口附带“按 2 个缺轮逐轮复核”的替代解释。正式串行流程按替代解释
确认该事件，随后继续恢复 `1722`、`1714` 和 `1580`，没有被局部移动主解释堵塞。

## 终局边界

自然停止后剩余 7 个事件：`mon061` 3 个、`mon142` 2 个、`mon271` 2 个。
三个直接前沿中，两个因局部移动证据不足安全拒答；`mon142:1763` 为操作竞争且窗口
只差 1 年。它们不是理论不可辨识事件，但本轮不继续放宽门槛，以免损害 clean review。

## 回归验证

- 相关 Vitest：5 个测试文件，144 项通过。
- `yarn validate:co612-recovery-regression`：首轮正确窗口 29，门槛 22，通过；
  joint 裁决与生产输出 45/45 完全一致；旧冻结输入 clean review 2/55。
- `yarn build`：通过。

