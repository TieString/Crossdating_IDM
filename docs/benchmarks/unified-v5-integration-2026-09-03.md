# v5 应用接入验收

## Material Passport

- Origin Skill: experiment-agent
- Origin Mode: validate
- Origin Date: 2026-09-03
- Verification Status: ANALYZED

已将用户接受的单一 v5 接入 `D:\Code\Crossdating_Tauri-experiment` 默认定年建议入口，并完成全量冻结校准回归：准确率93.23%、覆盖率99.83%、Clean误报0.99%，与Python研究链逐事件完全一致。本轮接入验收按用户修订目标通过。研究模型、207维特征及事件门保持冻结。`D:\Code\Crossdating_Tauri` 未修改。

## 同状态金标准

400个冻结状态，来自223个完整RWL文件。最终操作、精确位移、唯一窗口、partial的missing复核和可执行事件投影全部一致。

| 核对层 | 核对值数 | 结果 |
|---|---:|---|
| 标准化目标 | 118973 | 逐值完全一致 |
| 标准化master | 227180 | 逐值完全一致 |
| 逐参考芯 | 5196086 | 逐值完全一致 |
| 全302操作/位移的62维基础证据 | 7489600 | 逐值完全一致 |
| 三视图路径取样轨迹与删失路径 | 各9957600 | 逐值完全一致 |
| 全部候选207维特征 | 18035910 | 18035895个逐值相等；最大绝对差4.55×10⁻¹³ |
| 候选树模型分数 | 87130 | 逐值完全一致 |
| 按操作/位移汇总的分数 | 120800 | 逐值完全一致 |
| 最终输出与事件封装 | 400 | 400/400 |

路径轨迹对每状态每视图检查18个确定性时间行的全部461个lag状态，含首尾行；不是对全部时间点作逐点比较。全部候选特征、操作基础证据和最终输出均完整比较。报告：`unified-v5-ts-parity-400-v3.json`。

接入时修复了三项差异：SciPy最大值分离与成对求和引起的候选并列排序差异；编辑后缺少终止符导致的错误精度推断；旧封装器给负向whole自动附加缺轮解释。失败记录v1、v2和自检报告均保留，没有放宽数值门槛或重训模型。

## 应用路径与交互

- TypeScript在内存构建COFECHA testing-values证据，参考排除整个目标芯；全局G与局部delta分别处理。
- whole扫描−100…−1及+1…+100，partial扫描−100…−2；不再使用旧固定4/2个位移类别。
- 默认只输出一个操作。仅partial提供“以缺轮形式复核”；missing −1窗口独立计算，使用同一冻结状态。
- 状态内容SHA-256进入事件ID和缓存键；同状态复用结果。应用编辑后新状态有新事件ID，允许重新选择partial。
- 旧模型文件保留作审计，默认worker不再调用旧v3模型。新模型资产6,804,239字节，启动时校验SHA-256。

## 工程检查

`npx tsc --project tsconfig.engine-evidence.json --noEmit`通过；98项冻结模型、数值路径、精度、在线证据、复核、前沿协议和场景测试通过；`npm run build`通过。构建产物中的模型SHA-256与源资产相同。剩余构建警告为既有主chunk体积和Chart模块静态/动态重复导入。

额外使用真实`readRwlString`和`RwlEditor`审计co612：761个状态全部匹配冻结内容哈希，45条序列逐一恢复原始数据；不重跑COFECHA或模型。另补上显式−9999精度下末端真实宽度999的保护，12723个本轮机会/对照、12667个唯一目标均不受该边界修复影响；边界单元测试、严格检查及最终build再次通过。

## co612 完整回归

应用链687/716＝95.95%，覆盖708/716＝98.88%；单删352/358＝98.32%，串行335/358＝93.58%。原应用676/716，净增加11次正确；与冻结v5研究链761个状态结果全部相同。

首次直接正确636；partial→missing切换53次、恢复正确51；拒答8，操作/位移错误8，窗口错误13，提示更老事件6。45个Clean对照无误报。`mon041`单删5/5，串行按1977→1883→1879→1861→1778为5/5。

0端点、6–10和16–19年的小样本单元低于90%，保持失败标记。旧冻结co612 SHA-256为`5b1c4ba81068a81c1a6b6f9dcd8da79f7c2bbbdc5a8d0e9d188267969472c864`；用户当前文件为`bb71397bfe13c6444f2b75165c5fa8d42663fbd25c9ba3c88107e6c5a5bb94ca`。本次使用旧冻结状态，未修改当前用户文件。完整逐序列、single/serial、距离和错误明细见 `unified-v5-runtime-co612-audit.md`、同名JSON。

## 全量分层校准回归（已完成）

200个完整文件、6121个案例（3197个事件案例），共11962状态＝9038个真实事件机会＋2924个Clean。使用主A/B/C/D、三档文件r、干净目标masterCorrelation>0.60，D相邻局部事件≥30年。四个完整文件分片均完成，最长分片914.89秒；11962个状态与研究链的操作、位移、窗口、回答及工作流判分全部一致。

| 类别 | 正确/真实事件机会 | 窗口准确率 |
|---|---:|---:|
| A | 1158/1246 | 92.94% |
| B | 1093/1168 | 93.58% |
| C | 2000/2136 | 93.63% |
| D | 4175/4488 | 93.03% |
| 总体 | 8426/9038 | 93.23% |

总体回答9023、拒答15，操作/位移错误273、窗口错误324，首次直接正确8381；72次partial→missing切换恢复45次。总体操作准确率96.81%。全部局部主窗为13年。按完整文件bootstrap的窗口准确率95%区间为92.35%–94.01%，覆盖率区间99.73%–99.93%。Clean误报点估计0.99%，区间0.64%–1.37%。

三个文件r档准确率依次90.16%、93.10%、96.38%；最弱交叉单元D×0.60–0.70为89.54%，C×0.60–0.70为89.73%，均单列低于90%标记。whole精确位移为606/688＝88.08%，保留为操作弱项。整体接受不等于各细分单元均达93%。

状态缓存0命中/11962首次计算（全部状态唯一）；芯预处理571551次命中、17792次首次计算。完整12个事件交叉单元、3个Clean单元、全部距离段、1–15/15–34/16–34/中部汇总、逐文件区间及220个development/200个calibration文件和内容哈希见 `unified-v5-runtime-calibration-audit.md` 与同名JSON。

执行命令为 `scripts/benchmark-unified-v5-runtime.ts --manifest .benchmark-results/unified99-joint-global-v5/full-manifest.json --role calibration --primary --shard-count 4 --shard-index N --output docs/benchmarks/unified-v5-runtime-calibration-shard-N.json`。汇总使用 `scripts/summarize-unified-v5-runtime.py`，保留原始分片和全部失败行。

本轮没有训练、调门槛或消费新的final。结论限定为用户已接受的冻结校准总体和co612开发压力测试；新增登记目标未并入该总体，独立新final未开展。原失败报告保持原样，后续独立验证须使用新的完整文件。

## 开发启动内存问题补验

用户启动`yarn tauri dev`时，Vite根目录监听扫描了百万级`.benchmark-results`缓存，Node在约70秒耗尽4 GB堆。此前的build验证没有覆盖开发模式监听，这是一次验收遗漏。

`vite.config.ts`现排除`src-tauri`、`.benchmark-results`及`__pycache__`，不提高Node内存上限、不删除研究缓存、不改变模型。新增配置回归测试和`scripts/validate-vite-dev-watch.mjs`。

真实Vite配置在隔离端口1430运行150.33秒，加载249个项目源码模块、完成255次HTTP检查。源码HMR更新通过，模型资源SHA-256一致，实验缓存监听目录始终为0；稳态堆内存约60–62 MB，采样峰值88.28 MB。配置测试、配置TypeScript检查和`npm run build`均通过。临时服务和两个测试目录已清理，用户现有Tauri进程没有被关闭。

原始记录：`.benchmark-results/vite-dev-watch-check.json`。本次验证针对导致崩溃的前端开发服务，不冒充新增模型准确率实验或人工GUI全流程验收。
